#include "IRResponeCount.h"
#include"arduino.h"
/*#define DEF_IR_RECV_PIN_2   2
#define DEF_IR_RECV_PIN_3   3
#define DEF_IR_RECV_PIN_4   4
#define DEF_IR_RECV_PIN_5   5

CIRResponeCount  g_IrRecv2(DEF_IR_RECV_PIN_2);
CIRResponeCount  g_IrRecv3(DEF_IR_RECV_PIN_3);
CIRResponeCount  g_IrRecv4(DEF_IR_RECV_PIN_4);
CIRResponeCount  g_IrRecv5(DEF_IR_RECV_PIN_5);*/

void setup() {
  
}

void loop() {
    /*g_IrRecv2.IRRecvCount(2);
    g_IrRecv2.getCount();

    g_IrRecv3.IRRecvCount(2);
    g_IrRecv2.getCount();

    g_IrRecv4.IRRecvCount(2);
    g_IrRecv2.getCount();

    g_IrRecv5.IRRecvCount(2);
    g_IrRecv2.getCount();*/

}
